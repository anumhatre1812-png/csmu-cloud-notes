[phases.setup]
nixPkgs = ["nodejs_20", "typescript"]

[phases.install]
cmds = ["cd backend && npm install --include=dev"]

[phases.build]
cmds = ["cd backend && npx tsc"]

[start]
cmd = "cd backend && node dist/server.js"
